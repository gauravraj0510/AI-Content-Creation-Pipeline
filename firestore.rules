rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Require authentication for all operations
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
    
    // Alternative: More granular rules for specific collections
    // Uncomment and modify as needed
    
    /*
    // RAW_IDEAS collection - authenticated users can read/write
    match /RAW_IDEAS/{document} {
      allow read, write: if request.auth != null;
    }
    
    // FEED_METADATA collection - authenticated users can read/write
    match /FEED_METADATA/{document} {
      allow read, write: if request.auth != null;
    }
    
    // SUBREDDIT_METADATA collection - authenticated users can read/write
    match /SUBREDDIT_METADATA/{document} {
      allow read, write: if request.auth != null;
    }
    
    // SETTINGS collection - authenticated users can read/write
    match /SETTINGS/{document} {
      allow read, write: if request.auth != null;
    }
    */
  }
}
